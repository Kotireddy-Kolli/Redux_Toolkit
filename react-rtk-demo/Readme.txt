Integrating Redux to React,
    1. copy app and features folder from redux tooltik and add into react scr folder
    2. Change all the import and exports to es6
    3. install the react-redux library and Wrap the whole component in provider component
    4. Now export the store and pass as prop to provider
    5. Now we can use useSelector from the react-reduc library to access the state
    